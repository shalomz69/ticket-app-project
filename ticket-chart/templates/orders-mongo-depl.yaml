apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "ticket-chart.fullname" . }}orders-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "ticket-chart.fullname" . }}orders-mongo
  template:
    metadata:
      labels:
        app: {{ template "ticket-chart.fullname" . }}orders-mongo
    spec:
      containers:
        - name: {{ template "ticket-chart.fullname" . }}orders-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "ticket-chart.fullname" . }}orders-mongo-srv
spec:
  selector:
    app: {{ template "ticket-chart.fullname" . }}orders-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
