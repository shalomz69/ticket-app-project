apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "ticket-chart.fullname" . }}tickets-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "ticket-chart.fullname" . }}tickets-mongo
  template:
    metadata:
      labels:
        app: {{ template "ticket-chart.fullname" . }}tickets-mongo
    spec:
      containers:
        - name: {{ template "ticket-chart.fullname" . }}tickets-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "ticket-chart.fullname" . }}tickets-mongo-srv
spec:
  selector:
    app: {{ template "ticket-chart.fullname" . }}tickets-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
